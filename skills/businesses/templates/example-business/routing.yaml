brief_intake:
  default_employee: ceo

# TODO (ROUTING_METADATA_CONTRACT.md §7): derive one route per example_brief
# family in business.yaml. Each regex MUST fire on infinitive AND conjugated
# forms, EN and PT, accented and unaccented — use verb stems, never one form.
# No inline (?i) flag; matching is case-insensitive already. Example:
# auto_routes:
#   - pattern: '\b(escrev\w*|redig\w*|write|writing)\b.*\b(ebook|e-?book|livro)\b'
#     route_to: ceo
#     confidence_threshold: 0.75
auto_routes: []

mention_routing: []

ticket_intake:
  default_assignee: ceo
