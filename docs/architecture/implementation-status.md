# Estado da implementação

Data da revisão: 25 de agosto de 2026, sobre o HEAD `aae3100` da branch `feat/universal-runtime-glance-gauntlet`.

Este documento registra o que os cortes integrados na branch provam. Cada afirmação de estado aponta para um arquivo de teste ou script do repositório, com o título do teste quando a asserção é específica. O que não tem prova aparece como parcial ou pendente. Caminhos de teste sem prefixo ficam em `skills/harness/tests/`.

## Estado geral

| Área | Estado | Evidência | Limite atual |
|---|---|---|---|
| Política de runtime ativo | Implementada | `skills/squads/tests/active-runtime-policy.test.ts`: `active` dispensa `minimum`, usa adapter registrado ou bridge explícita e recusa runtime sem as features obrigatórias | Nenhuma enumeração central é exigida pela política `active`; `policy: negotiate` (RT-003) não tem implementação nem teste |
| Runtime Provider e Model Broker | Implementados e ligados aos Runs | `skills/squads/tests/runtime-broker.test.ts`: runtime desconhecido entra sem editar allowlist, seleção por capability e modalidade, recusa honesta sem modelo compatível, stale bloqueado sem permissão, fallback ao runtime ativo. `runtime-snapshot-after-catalog-update.e2e.test.ts`: snapshot congelado por Run e consultável depois de atualizar o catálogo | Catálogo opcional: sem descriptor o snapshot fica `resolved: false`; os canários não passam requisitos de features e modelo dos manifests; custo previsto não entra no snapshot |
| Run Kernel | Implementado e alimentado pelos dois modos do dispatch | `run-kernel.test.ts`: sequência monotônica, causalidade, idempotência, outbox, scopes, ArtifactRef e facade legada. `standard-publication.test.ts` e `dispatch-standard-kernel.e2e.test.ts`: dual-write do modo `standard` nas três branches do `dispatch.ts` | Publicação fail-open (`x_run_kernel_unavailable`); rota com vários Squads publica sob o primeiro; ArtifactRef só é registrado para candidates do Gauntlet, não para deliverables do modo `standard`; nenhum teste compara run-ledger legado e kernel para o mesmo Run |
| Gauntlet Engine | Engine implementada, canário nas três intensidades, multi-target por padrão com kill switch, julgamento agêntico obrigatório: avaliador por executor real (squad instalado ou o `judge-x` do engine, identidade própria e prompt enxuto) com contrato de scorecard, seleção auditada, cota de avaliação com piso, heurística só por opt-in explícito e Gauntlet que não inicia sem juiz | `gauntlet-compiler.test.ts`, `gauntlet-controller.test.ts`, `gauntlet-store.test.ts`, `gauntlet-revision-loop.e2e.test.ts`, `agent-x-gauntlet-cutover.test.ts`, `gauntlet-evaluator-adapter.test.ts`, `gauntlet-evaluator-selection.test.ts`, `gauntlet-evaluator-dispatch.e2e.test.ts`, `judge-x.test.ts`, `judge-x-dispatch.e2e.test.ts`, `doctor-evaluator.test.ts`, `multi-target-cli.test.ts`, `multi-target-dispatch-adapters.test.ts`, `run-kernel-multi-target-ports.test.ts` | `nrv multi-target run` desliga por `NIRVANA_MULTI_TARGET_KILL_SWITCH=1`; os adapters leem só o exit code do nó; paradas `max_duration` e `judge_disagreement` sem teste; sem waiver auditável para autoavaliação; `budget_exhausted` só é nomeado no claude-code, o único runtime com teto de gasto no CLI |
| Glance Project Workspace | Control plane com execução real por processo filho e a cascata do maestro a partir da Message | `glance-control-plane.test.ts`, `project-control-plane.test.ts`, `glance-agent-x-canary-e2e.test.ts`, `agent-x-canary-recovery.test.ts`, `glance-execution-runner.test.ts`, `glance-message-route.test.ts`, `glance-run-event-labels.test.ts`, `glance-multi-target-projection.test.ts` | Uma Message por vez por servidor; uma rota do roteador com dois ou mais squads cai em `agent-x`, porque um Run do Glance executa um alvo; a projeção multi-target só existe depois do primeiro snapshot do coordenador; a aba Runs deriva do audit legado; nenhum teste de acessibilidade |
| Cutover vertical | Loop completo nos três targets | `gauntlet-revision-loop.e2e.test.ts`, `business-gauntlet-glance-proof.e2e.test.ts`, `business-delivery-parity.e2e.test.ts` | Rotas com múltiplos Squads e o coordenador multi-target continuam fora do loop; o chat do Glance pede `light` para `agent-x` e deixa business e squad decidirem o modo pelo env do servidor |
| Gate organizacional | Implementado em `check:all` | `scripts/check-organizational-non-regression.ts --strict`, `organizational-non-regression.test.ts`, `skills/_shared/tests/tree-digest.test.ts` | Protege as raízes instaladas durante as suítes de entidades; não observa execuções de dispatch |
| Núcleo de configuração | Implementado: schema único, resolução variável > projeto > global > engine > padrão, leitores migrados, spawners fixando o efetivo nos filhos, `nrv config`, seção `config` do doctor | `skills/_shared/tests/settings-schema.test.ts`, `skills/_shared/tests/settings.test.ts`, `settings-readers.test.ts`, `settings-spawners.test.ts`, `config-command.test.ts`, `doctor-config.test.ts`; contrato em [Configuração](configuration.md) | O painel "Configuração" do Glance edita o núcleo por `GET/PUT/DELETE /api/v1/settings` e a mudança vale no próximo despacho (`glance-settings-api.test.ts`, `glance-settings-panel.test.ts`; contrato em [Configuração pelo Glance](glance-settings.md)); a seção `.env` continua para segredos, escopo e `LLM_CASCADE`; `glance.execution` e `updates.check` valem no próximo boot |

## O que já funciona

### Runtime universal

A política `active` aceita o runtime da sessão quando há provider ou bridge compatível. Um runtime novo pode entrar pelo catálogo sem alteração de allowlist no core. O Model Broker filtra capabilities e modalidades antes de ordenar modelos. Catálogos vencidos falham de forma explícita, salvo autorização específica para aceitar stale data.

Todo Run dos três canários e do multi-target congela a decisão de execução no journal por `freezeExecutionSnapshot` (`skills/harness/lib/runtime-snapshot.ts`), que consulta o broker sobre os catálogos em `NIRVANA_PROVIDER_CATALOG_DIR`, `~/.nirvana/providers` e `<projectRoot>/.nirvana/providers`. Com descriptor compatível, o snapshot traz runtime com versão, provider, modelo, evidência do broker e catálogo consultado. Quando provider ou modelo continuam sob escolha interna do runtime, o snapshot registra `resolved: false` e `runtime-default` em vez de inventar uma identidade. O digest dessa estrutura fica em `policySnapshotRef`.

Catálogo vencido marca `catalog.stale: true` e deixa a execução seguir sem resolução; `NIRVANA_ALLOW_STALE_CATALOG=1` aceita o dado vencido com aviso. Feature obrigatória ausente ou provider sem modelo compatível encerra o Run em `rolled_back` com `reason: runtime_incompatible` antes do producer e grava `x_runtime_incompatible` no audit legado; não há troca silenciosa nem fallback para o executor anterior. A prova hermética `runtime-snapshot-after-catalog-update.e2e.test.ts` reescreve o catálogo entre dois Runs e confirma que o primeiro devolve o mesmo `policySnapshotRef` e o mesmo payload pelo kernel e pelo Glance depois de um restart. Detalhes em [Snapshot de runtime](runtime-snapshot.md).

Manifests legados continuam usando `declared`. A ausência de `runtime_requirements` não cria obrigação de instalar Claude Code, Codex, Gemini CLI ou outro produto enumerado.

### Run Kernel

O kernel mantém eventos append-only em SQLite, sequência monotônica por Project, idempotência, causalidade, projeções reconstruíveis e outbox durável. Mensagens visíveis ficam separadas do journal técnico. ArtifactRef registra digest, tamanho, origem e revisão sem incorporar o conteúdo privado ao evento.

A facade de compatibilidade é opt-in. Ela não altera readers legados por simples presença do módulo.

O modo `standard` do `dispatch.ts` publica cada execução com `--exec` como Run canônico, nas três branches (business, squad-only e `agent-x`). O módulo `lib/run-kernel/standard-publication.ts` abre o kernel do projeto (o mesmo dos canários: `outputs/<pid>/.nirvana/run-kernel.sqlite`, ou o do root do projeto com `--run-id`), cria o Run tipado ou adota o que o Glance preparou, grava `runtime.selection_snapshot` com o snapshot congelado pelo broker e transiciona `prepared → running` antes do executor, `running → verifying` antes da delivery pipeline e o estado terminal pelo resultado da entrega: exit 0 com gate `pass` vira `completed`; exit 0 com `fail-forced` ou `fail-accepted` vira `delivered_with_reservations`; exit 2 ou 3 vira `withheld`; exit 1 ou erro vira `failed` com `payload.error`. O payload terminal registra `exitCode`, `gateOutcome` e `outputsRoot`, e as chaves idempotentes usam o prefixo `standard:<runId>:`. Snapshot com erros do broker encerra o Run em `rolled_back` com `reason: runtime_incompatible` antes do producer e o dispatch sai com 1, a mesma regra dos canários (RT-002).

O executor legado, a delivery pipeline, os exit codes, os artifacts, o audit e o session file não mudaram; o run-ledger continua aberto pelo próprio dispatch, por isso a publicação escreve no kernel diretamente, sem a facade com adapter legado. Kernel indisponível (disco, permissão, Run adotado em estado que recusa a transição) registra `x_run_kernel_unavailable` no audit e o dispatch prossegue exatamente como antes. Sem `--exec`, ou com argumentos inválidos, nenhum Run é criado. Provas: `standard-publication.test.ts` (mapeamento terminal, adoção, idempotência, fail-open) e `dispatch-standard-kernel.e2e.test.ts` (o `dispatch.ts` real com runtime falso para `agent-x` e Squad, adoção por `--run-id`, falha do runtime, scaffold sem Run).

### Gauntlet Engine

O compiler produz planos determinísticos para `light`, `balanced` e `exhaustive`. O controller bloqueia nova rodada sem orçamento, impõe duração e quantidade máxima de rounds, detecta falta de progresso, impede autoavaliação e registra candidates e scorecards imutáveis.

Essas garantias foram testadas com adapters determinísticos. No CLI de produção, `--exec` com `gauntlet` explícito substitui o executor legado pelo controller para `agent-x`, um Squad e Businesses allowlistados. `light`, `balanced` e `exhaustive` usam o mesmo loop: `candidateStrategy.count` candidates isolados por rodada, avaliação independente de todas as revisões, revisão causal por `reviseCandidate` quando o controller pede, seleção por `selectedRevisionId` e gate final apenas sobre a revisão selecionada. A prova hermética `gauntlet-revision-loop.e2e.test.ts` cobre a revisão causal, as paradas finitas, a retomada após crash e os três targets.

O engine multi-target tem entrada pública escriturada: `nrv multi-target plan|run|status` compila um plano em arquivo (`nirvana.multi-target-plan/v1alpha1`), cria o Run no kernel do projeto, executa as ondas pelas portas do Run Kernel e pelos adapters de dispatch, e transiciona `prepared → running → verifying → completed|withheld|failed`. O `run` executa sem variável, `NIRVANA_MULTI_TARGET_KILL_SWITCH=1` o desliga, e repetir o comando retoma sem reexecutar nós concluídos. O dispatch ganhou `--business`, `--squad` e `--agent-x`, então os adapters selecionam o alvo sem chamada ao roteador. Detalhes em [Comando multi-target](gauntlet-multi-target-cli.md).

### Business no canário

O pós-gate de Business passa por `runBusinessPostGate`, fronteira reutilizável chamada pelo mesmo hook da delivery pipeline. A extração preserva PDF, HTML, ZIP, manifesto, session file, audit e decisões terminais (`business-post-gate.test.ts`). O canário Business usa essa fronteira no gate final.

A prova hermética `business-delivery-parity.e2e.test.ts` compara o contrato legado congelado com a nova fronteira. No sucesso, ela atravessa manifesto, gate offline, PDF, HTML, ZIP e session file, então compara resultado, arquivos, eventos e estado terminal `delivered`. Na falha de manifesto, ambos os caminhos terminam em `failed`, sem publicação nem evento `delivered`. A prova não chama LLM, runtime externo, credenciais ou rede.

O teste integrado `business-gauntlet-glance-proof.e2e.test.ts` fecha a etapa seguinte com um target `business` tipado: produção local, candidate e scorecard pelo `GauntletController`, evaluator independente, snapshot honesto de runtime, provider e modelo, delivery final, pós-gate e leitura da timeline pelo Glance. Os eventos de delivery preservam causalidade e sequência. Uma rejeição bloqueia delivery e pós-gate.

O canário Business é estritamente opt-in. Ele exige pedido explícito de `gauntlet`, execução individual e slug presente em `NIRVANA_BUSINESS_GAUNTLET_ALLOWLIST`, em qualquer das três intensidades. `NIRVANA_BUSINESS_GAUNTLET_KILL_SWITCH=1` desliga o canário antes da produção. Ausência de qualquer condição mantém o executor legado (`business-gauntlet-canary-policy.test.ts`).

Rollback automático só ocorre antes do producer iniciar. Depois desse marco, erro ou rejeição encerra o Run canônico e nunca chama o producer legado. Os eventos `x_business_gauntlet_selected`, `x_business_gauntlet_bypassed`, `x_business_gauntlet_rollback` e `x_business_gauntlet_terminal` registram a decisão operacional. O rollout não inclui modo `auto`, team mode, múltiplos Squads ou troca automática de runtime. A allowlist vazia mantém todos os Businesses no fluxo anterior.

### Glance

Projetos adotados têm identidade estável em `.nirvana/project.yaml`. Adoção exige preview e `plan_hash`. Conversations e Messages sobrevivem a reinício. Runs e Events usam o Run Kernel, a timeline é ordenada por sequência e SSE retoma com `Last-Event-ID`.

O servidor restringe ações a loopback, valida Origin, exige `Idempotency-Key` nas escritas e rejeita path traversal. O navegador não recebe uma rota de shell arbitrário.

Uma Message de projeto adotado prepara um Run com `policySnapshotRef: gauntlet-light-canary`, persiste o vínculo e entra numa fila serial. Com `nrv glance` em produção, a fila roda cada Run em um processo filho do `dispatch.ts` (`--agent-x`, `--business` ou `--squad`, mais `--run-id` para adotar o Run já preparado no kernel do projeto), e o servidor segue respondendo durante a execução. O texto `use business <slug>:` ou `use squad <slug>:` no início da Message escolhe o alvo sem roteador; qualquer outro texto passa pelo `agenticRoute` antes de o Run existir e chega à empresa ou ao squad que ele aponta, ou a `agent-x` no Gauntlet `light` quando nada cobre, com a decisão em `run.route` e no audit com o `trace_id` da Message. A UI não inicia o executor legado em paralelo. Projeto não adotado conserva o comportamento anterior. Runtime ausente e cancelamento antes de side effects produzem estados terminais explícitos; `--read-only` e `NIRVANA_GLANCE_EXECUTION=0` sobem o cockpit sem runner.

O chat canônico não prepara Runs por padrão: cada Message é um turno do maestro (`mode: "turn"`), o runtime do projeto rodando headless na raiz do projeto com a sessão nativa da conversa (`session_id`, `session_runtime`, `session_started_at`, `last_turn_at` persistidos em `conversations`), streaming por SSE, resposta gravada uma vez, custo no audit como `cost_emission`, cancelamento por grupo de processos (`cancelled`, nunca `failed`) e fila serial por conversa; `mode: "run"` mantém o caminho do Run. Prova: `glance-maestro-turn.test.ts`. Detalhes em [O turno do maestro](glance-execution.md#o-turno-do-maestro) e [Sessões do maestro](maestro-sessions.md).

Cancelar um Run em execução envia o sinal ao grupo de processos do filho, então o runtime neto (`claude -p`, `codex`) morre com o `dispatch.ts`, e conclui `running → cancelling → cancelled`. Um filho que sai sem transição terminal deixa o Run `failed` com `child_exited_without_terminal_state`. Os eventos `glance.child_started`, `glance.child_exited` e `glance.child_killed` registram pid, tentativa e código de saída na timeline. Uma Message com `use business <slug>:` ou `use squad <slug>:` em modo `standard` termina em estado terminal real, porque o filho publica o Run adotado pelo mesmo módulo de publicação do dispatch.

No restart, Runs `prepared` vinculados a uma Message são reivindicados atomicamente e reencaminhados por `runId`. Runs em execução com filho registrado são reanexados quando o pid está vivo (`canary.recovery_reattached`) ou redespachados com o mesmo `--run-id` quando o pid morreu (`canary.recovery_redispatched`); o cutover retoma sem repetir producer nem evaluator, e dois restarts sobre o mesmo processo morto geram um único filho novo. Runs terminais e Runs em execução sem evento de filho são ignorados e registrados de forma explícita. Detalhes em [Execução no Glance](glance-execution.md).

A timeline do chat rotula todo evento canônico do Run Kernel por `type`, com título, subtítulo e tom em PT-BR, a partir de um único módulo (`run-event-labels.js`) compartilhado pela página e pelos testes. Eventos legados do audit continuam resolvendo pelo mapa antigo, e um tipo desconhecido aparece com o próprio nome, nunca sem título. Snapshots do coordenador e renovações de lease permanecem no stream, mas ficam ocultos até o usuário pedir para vê-los. O cabeçalho do Run mostra estado terminal, decisão do Gauntlet e custo vindos dos eventos canônicos.

`GET /api/v1/runs/:run/multi-target` reconstrói a projeção do coordenador multi-target a partir do journal, com a mesma reaplicação de eventos que as portas do Run Kernel usam no reload. O painel do Run lista ondas, nós, estado, custo concedido e reportado, razão e bloqueios. Detalhes em [Timeline canônica no Glance](glance-canonical-timeline.md).

## Expansão vertical necessária

Os oito passos do contrato vertical, com estado e prova. `concluído` significa que um teste cobre o passo de ponta a ponta; `parcial` significa que só parte do passo tem prova.

1. O Glance recebe uma Message e prepara um Run canônico. Estado: concluído. Prova: `glance-agent-x-canary-e2e.test.ts`, "message prepares one linked run, executes through adapter and survives restart" e "a Message runs in a child dispatch and the full canonical timeline streams over SSE, resumable by Last-Event-ID".
2. O harness resolve Business, Squad ou `agent-x` sem mudar a cascata atual. Estado: concluído. Prova: `dispatch-explicit-target.test.ts` (as flags `--business`, `--squad` e `--agent-x` resolvem a rota sem o roteador e são mutuamente exclusivas); `glance-execution-runner.test.ts` ("`use business <slug>:` and `use squad <slug>:` prepare typed targets, case-insensitive on the keyword"; "anything else names no target: agent-x here, and the queue asks the router before preparing the Run"); `glance-message-route.test.ts`, com o roteador injetado e sem LLM: "(a) an explicit prefix wins: the router is never asked and the decision is recorded as explicit", "(b) a decision with primary_business prepares a business Run with capability business.dispatch", "(c) a squad-only decision prepares a squad Run with squad.execute; several mandatory squads fall to agent-x", "(d) no_match keeps the Message on agent-x, recorded as fallback with the router's reason", "(e) a router that throws or hangs falls to agent-x and agentic_route_failed carries the Message's trace", "(f) routing.mode=fast skips the router; routing.on_router_failure=fail refuses the Run instead of falling to agent-x" e "a Message without a prefix reaches the business the injected router names, and the standard child runs it to a terminal state" (servidor real, filho fake com `--business web-studio`, `run.route` no recibo e `auto_route_selected` com o `trace_id` no audit do projeto). Limite: um Run do Glance executa um alvo, então uma rota com vários squads cai em `agent-x`.
3. O broker congela runtime, provider, modelo, catálogo e evidências numa policy snapshot. Estado: concluído. Prova: `runtime-snapshot-after-catalog-update.e2e.test.ts`; `agent-x-gauntlet-cutover.test.ts`, "freezes the runtime and model decision in the canonical journal"; `standard-publication.test.ts`, "a fresh Run walks prepared → snapshot → running → verifying → terminal with standard keys". Limite: requisitos de features e modelo não vêm dos manifests.
4. O modo `standard` chama o executor atual e publica transições no Run Kernel. Estado: concluído. Prova: `dispatch-standard-kernel.e2e.test.ts` (dispatch real, runtime falso, três branches) e `standard-publication.test.ts`.
5. O modo `gauntlet` chama o controller, cria workspaces isolados por candidate e seleciona evaluators independentes por capability. Estado: concluído. Prova: `gauntlet-revision-loop.e2e.test.ts`, "balanced produces three isolated candidates, evaluates all and publishes only the selected one" e "judge-x, through the dispatch evaluator, judges an agent-x producer across the causal revision to completed"; `gauntlet-compiler.test.ts`, "selects by capability without hardcoded squads and rejects self-evaluation"; `agent-x-gauntlet-cutover.test.ts`, "rejects a producer evaluating its own candidate and records a post-start failure"; `gauntlet-evaluator-selection.test.ts`, "never falls to the heuristic: without a judge the selection is unavailable". Limite: holdout é metadata; o evaluator de produção é um squad instalado com `quality.specification_conformance` ou o `judge-x` do engine, e o gate offline só por opt-in explícito.
6. Cada avaliação produz scorecard ligado a evidências; revision recebe defeitos causais. Estado: concluído. Prova: `gauntlet-revision-loop.e2e.test.ts`, "revises the candidate from its evaluated defects and reaches the final gate" (`failedDimensions`, `evaluationIds` e `revisionRequests` com `evidenceRefs` chegam ao `reviseCandidate`; a revisão grava `parentRevisionId` e `causalEvaluationIds`); `gauntlet-store.test.ts`, "keeps candidate revisions immutable and causal".
7. O quality gate final permanece obrigatório depois da seleção. Estado: concluído. Prova: `gauntlet-controller.test.ts`, "delivers only after an independent hard gate passes"; `gauntlet-revision-loop.e2e.test.ts`, "withholds a revision that regresses a passed blocking dimension without running the final gate" e `finalGates: 1` em cada sucesso; `business-gauntlet-glance-proof.e2e.test.ts`, "withholds a rejected Business candidate before delivery and keeps production cutover disabled".
8. O Glance acompanha a mesma sequência de eventos, sem reconstruir verdade a partir de logs soltos. Estado: concluído. Prova: `glance-agent-x-canary-e2e.test.ts` (treze eventos de `run.prepared` a `glance.child_exited` por SSE, ids contíguos); `business-gauntlet-glance-proof.e2e.test.ts` (timeline com os mesmos `sequence` do kernel e causalidade por `causationId`); `glance-run-event-labels.test.ts`; `glance-multi-target-projection.test.ts`. Limite: a aba Runs ainda deriva do audit legado.

O modo `standard` continua padrão. O cutover Gauntlet exige pedido explícito e `--exec`, e o rollback para o executor legado só acontece antes do primeiro producer (`business-gauntlet-canary-policy.test.ts`).

## Critérios para declarar o programa completo

| # | Critério | Prova | Estado |
|---|---|---|---|
| 1 | Um teste E2E cria Project, Conversation, Message e Run, despacha um target real por adapter fake e observa toda a timeline por SSE | `glance-agent-x-canary-e2e.test.ts`, "a Message runs in a child dispatch and the full canonical timeline streams over SSE, resumable by Last-Event-ID": projeto adotado, conversa e Message por HTTP, filho fake injetado por `NIRVANA_DISPATCH_SCRIPT` executando o cutover em outro processo, treze eventos consumidos pelo stream, retomada a partir do id 6, um único producer, artifact em disco. O mesmo arquivo cobre Squad e Business tipados em modo `standard` | atendido |
| 2 | `standard` mantém exit codes, artifacts, audit e session files atuais | `dispatch-standard-kernel.e2e.test.ts`: exit 0, 1 e 3 preservados, `report.html` no `outputs-root`, eventos legados `dispatch_agent_x`, `agent_executed`, `verify_passed`, `gate_passed` e `delivered` presentes, nenhum `x_run_kernel_unavailable`. `business-delivery-parity.e2e.test.ts`, "matches the legacy observable contract through manifest, gate and publication": resultado, arquivos, eventos e `session.json` iguais entre o caminho legado e a fronteira nova. `standard-publication.test.ts`, "an unavailable kernel is fail-open: x_run_kernel_unavailable, no exception, every later call a no-op" | atendido |
| 3 | `gauntlet light` executa produção, avaliação independente, revisão causal e gate final para Business, Squad e `agent-x` | `gauntlet-revision-loop.e2e.test.ts`: "revises the candidate from its evaluated defects and reaches the final gate" (um candidate, evaluator independente, revisão causal, gate final uma vez), "a typed %s producer crosses the revision loop to completed" para `agent-x` e `squad`, "a typed Business crosses the revision loop, the real offline gate and the post-gate" | atendido |
| 4 | Reiniciar depois de `candidate_created` retoma sem repetir side effect | `agent-x-gauntlet-cutover.test.ts`, "resumes after interruption without dispatching the persisted candidate twice" (crash em `afterCandidatePersisted`, segunda execução conclui com um único producer); `gauntlet-revision-loop.e2e.test.ts`, "resumes after a crash before the re-evaluation without producing the revision again"; `agent-x-canary-recovery.test.ts`, "a running run whose child is dead is redispatched once across two restarts and resumes without repeating the producer" (um `candidate_created`, um producer, um gate final, dois restarts) | atendido |
| 5 | Budget insuficiente impede a próxima execução antes de reservar recursos | `gauntlet-controller.test.ts`, "does not dispatch a round without budget" (`beginRound` devolve `max_cost` e a rodada permanece em 0); `agent-x-gauntlet-cutover.test.ts`, "rolls back when the light budget blocks execution before the candidate" (zero producers, zero gates, Run `rolled_back`); `gauntlet-aggregate-budget.test.ts`, "rejects when the aggregate cap cannot fund every safe minimum" | atendido |
| 6 | Snapshot de runtime e modelo permanece consultável mesmo depois de atualizar o catálogo | `runtime-snapshot-after-catalog-update.e2e.test.ts`, "a Run keeps the snapshot it froze after the catalog gains a better model, in the kernel and in Glance across a restart" | atendido |
| 7 | Projeto legado abre sem escrita e só migra após adoção explícita | `glance-control-plane.test.ts`, "legacy discovery is read-only and adoption is explicit" (`project.yaml` ausente depois da listagem; adoção com `plan_hash` responde 201); `project-control-plane.test.ts`, "plans without writes and adopts only with the current plan hash" (hash antigo recusado, arquivo do usuário preservado) | atendido |
| 8 | Testes de businesses, squads e mind-clones existentes passam sem migração destrutiva | `bun scripts/check-organizational-non-regression.ts --strict` nesta rodada: 36.292 entradas nas raízes instaladas, suítes de entidades com 249 pass, 1 skip e 0 fail em 27 arquivos, 0 caminhos alterados. O gate é provado por `organizational-non-regression.test.ts` e `skills/_shared/tests/tree-digest.test.ts` | atendido |

## Resultado de testes nesta integração

Rodada sobre o HEAD `aae3100`, em 25 de agosto de 2026, nesta máquina (macOS, Bun 1.3.6).

| Comando | Resultado |
|---|---|
| `bun test skills` | 1.222 pass, 1 skip, 0 fail; 5.018 asserções; 1.223 testes em 133 arquivos; 77,7 s. O `routing-eval.test.ts` mediu 3.449 casos com top-1 de 98,5%, top-3 de 99,5% e MRR de 0,989 |
| `bun test skills/harness` | 973 pass, 0 fail; 4.253 asserções; 106 arquivos; 87,6 s |
| `bun run check:all` | Passam, nesta ordem: paridade de comandos do SKILL.md, paridade de eventos de audit, fonte em inglês (444 arquivos de código e 44 `.md` de instrução), paridade de changelog (51 versões, 121 seções), paridade de versão (0.7.11 em `package.json`, `skills/VERSION` e `CHANGELOG.md`) e coverage ratchet (255 entidades). Falha em `check-not-for-fires.ts --strict`: 300 de 2.080 fences `not_for` mortas (14%) em 105 entidades instaladas, 37 entidades com a maioria morta e `tessera-acquisition` chegando ao teto com 1 de 5 mortas. A causa é o conteúdo das entidades instaladas em `~/squads` e `~/businesses`, não código do repositório. O encadeamento por `&&` interrompe o script nesse passo |
| Passos de `check:all` depois do `not_for`, rodados um a um | Todos passam: `check-capability-clones` (22 capabilities compartilhadas, 70 instâncias), `check-clone-bindings` (61 businesses, 154 bindings, 558 clones), `check-engine-purity`, `sync-agent-docs --check` (`CLAUDE.md` e `GEMINI.md` em sincronia), `check-dispatch-contract` (5 arquivos de protocolo, 8 exemplos, 11 regras) e o gate organizacional abaixo |
| `bun scripts/check-organizational-non-regression.ts --strict` | OK: 36.292 entradas nas raízes `~/businesses`, `~/businesses/_library/dna` e `~/squads`; suítes de entidades com 249 pass, 1 skip e 0 fail em 27 arquivos (3,2 s); 0 caminhos alterados |
| `bun skills/harness/scripts/validate.ts` | 55 checks: 47 pass, 8 avisos, 0 fail; sai com código 1 por causa dos avisos. Os avisos são ambientais: `kimi`, `qwen` e `opencode` fora do PATH; skills expostas também por `~/.agents/skills`; registries de squads, businesses e mind-clones com 32 h; corpus 63% em português |
| `git diff --check` | Sem achados |

## Regra de entrega

Os oito critérios desta página têm prova. O critério global de aceite de [Requisitos executáveis](executable-requirements.md) não está atendido: RK-001, RK-002, RK-004, RK-005, RT-006, GT-002, GT-005, GT-007, GL-001 e GL-004 estão `[parcial]`, e RT-003 e GL-006 continuam `[proposto]`, sem prova.

O que o sistema opera hoje: modo `standard` nas três branches do dispatch, com Run canônico publicado no kernel; Gauntlet por pedido explícito para `agent-x`, um Squad e Business allowlistado, nas três intensidades, com revisão causal e gate final; Messages do Glance executadas por processo filho, com cancelamento que alcança o runtime e recuperação por pid após restart; `nrv multi-target plan|run|status` por padrão, com `NIRVANA_MULTI_TARGET_KILL_SWITCH=1` para desligar.

O que permanece fora: team mode; rotas com múltiplos Squads no Gauntlet; cancelamento conjunto de irmãos e recovery humano no coordenador multi-target; modo `auto`; requisitos de features e modelo lidos dos manifests; `policy: negotiate`; gate WCAG 2.2 AA no cockpit; ações de Run além de cancelar (follow-up, steer, retry, revise, fork, approval); ArtifactRef para deliverables do modo `standard`. Até esses itens ganharem prova, a descrição correta é: fundação integrada, modo `standard` publicado no kernel e Gauntlet operacional por opt-in nos três targets.
