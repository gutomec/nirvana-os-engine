# Examples

One end-to-end walkthrough of what Nirvana-OS actually does when an agent
dispatches a brief. It is written for both readers: the **human** sees the shape
of a run; the **agent** sees the exact contract to follow.

- [`dispatch-walkthrough.md`](./dispatch-walkthrough.md) — a single brief from
  prose to delivered artifact, with the audit trail it must leave behind.
