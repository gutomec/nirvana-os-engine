# CLA signatures

Storage branch for the CLA bot (`contributor-assistant/github-action`). Each
signature on a pull request is appended to `signatures/cla.json` here.

Do not protect this branch — the bot needs to write to it. Nothing else lives
here; the project source is on `main`.
